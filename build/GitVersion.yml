assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-scheme: MajorMinorPatch
mode: ContinuousDelivery
next-version: '13.0'
commit-message-incrementing: Enabled
branches:
  develop:
    regex: ^develop-u13[/-]?
  main:
    regex: ^main-u13[/-]?
  release:
    regex: ^releases?-u13[/-]?
    mode: ContinuousDeployment
  feature:
    regex: ^features?(-u13)?[/-]?
  pull-request:  
    regex: ^(pull|pull\-requests|pr)(-u13)?[/-]?
  hotfix:
    regex: ^hotfix(es)?-u13[/-]?
  support:
    regex: ^support(-u13)?[/-]?
ignore:
  sha: []
merge-message-formats: {}
