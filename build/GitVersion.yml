assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-scheme: MajorMinorPatch
mode: ContinuousDelivery
next-version: 9.0
commit-message-incrementing: Enabled
branches:
  develop:
    regex: ^develop-u9[/-]?
  main:
    regex: ^main-u9[/-]?
  release:
    regex: ^releases?-u9[/-]?
    mode: ContinuousDeployment
  feature:
    regex: ^features?(-u9)?[/-]?
  pull-request:  
    regex: ^(pull|pull\-requests|pr)(-u9)?[/-]?
  hotfix:
    regex: ^hotfix(es)?-u9[/-]?
  support:
    regex: ^support(-u9)?[/-]?
ignore:
  sha: []
merge-message-formats: {}
