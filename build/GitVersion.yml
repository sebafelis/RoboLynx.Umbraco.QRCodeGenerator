assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-scheme: MajorMinorPatch
mode: ContinuousDelivery
next-version: 12.0
commit-message-incrementing: Enabled
branches:
  develop:
    regex: ^develop-u12[/-]?
  main:
    regex: ^main-u12[/-]?
  release:
    regex: ^releases?-u12[/-]?
    mode: ContinuousDeployment
  feature:
    regex: ^features?(-u12)?[/-]?
  pull-request:  
    regex: ^(pull|pull\-requests|pr)(-u12)?[/-]?
  hotfix:
    regex: ^hotfix(es)?-u12[/-]?
  support:
    regex: ^support(-u12)?[/-]?
ignore:
  sha: []
merge-message-formats: {}
