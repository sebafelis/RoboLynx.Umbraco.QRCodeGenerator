assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-scheme: MajorMinorPatch
mode: ContinuousDelivery
next-version: 10.0
commit-message-incrementing: Enabled
branches:
  develop:
    regex: ^develop-u10[/-]?
  main:
    regex: ^main-u10[/-]?
  release:
    regex: ^releases?-u10[/-]?
    mode: ContinuousDeployment
  feature:
    regex: ^features?(-u10)?[/-]?
  pull-request:  
    regex: ^(pull|pull\-requests|pr)(-u10)?[/-]?
  hotfix:
    regex: ^hotfix(es)?-u10[/-]?
  support:
    regex: ^support(-u10)?[/-]?
ignore:
  sha: []
merge-message-formats: {}
